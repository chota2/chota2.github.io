var header = {
    title : '히더'
}

window.addEventListener('DOMContentLoaded', (event) => {
    console.log('HEADER DOM fully loaded and parsed');

    const element1 = document.getElementById("header");

});

export {header};